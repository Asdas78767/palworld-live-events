# AGENTS.md

## 0. 적용 범위와 버전 기준

이 저장소는 **치지직 후원 이벤트를 Palworld 전용 서버의 인게임 이벤트로 변환하는 연동 시스템**이다.

고정 기준:

- 대상 게임/전용 서버 버전: `Palworld v1.0.1`
- Palworld REST 기준: 구현 시점의 공식 Server Guide 개별 엔드포인트 문서와 실제 서버 `/info` 응답을 함께 확인한다. `2026-07-17` 확인 기준 개별 주요 REST 엔드포인트 페이지는 `1.0.0`을 표시하지만, 카테고리 색인에는 다른 버전 표기가 나타날 수 있으므로 색인 버전만으로 명세를 고정하지 않는다.
- 치지직 연동 기준: CHZZK Developers의 현재 공식 `Authorization` 및 `Session` 문서
- 공식 문서에서 확인되지 않은 엔드포인트, 필드, 아이템 ID, 팰 ID, 모드 API는 존재한다고 가정하지 않는다.

`v1.0.1`은 저장 데이터가 특정 조작 뒤 의도치 않게 폐기될 수 있던 문제를 수정한 패치다. 이 수정이 모든 저장 손상 가능성을 제거한다고 해석하지 않는다. 모드 설치, 서버 재시작, 설정 파일 변경, 저장 파일 처리에서는 아래 P0 규칙을 항상 적용한다.

---

## 1. 우선순위

규칙이 충돌하면 숫자가 낮은 항목을 우선한다.

| 우선순위 | 영역 | 판단 기준 |
|---|---|---|
| **P0** | 저장 데이터·보안·파괴적 작업 | 월드 손상, 계정 탈취, 무단 서버 제어를 방지한다. |
| **P1** | 공식 API 정확성 | 공식 문서에 존재하는 기능과 필드만 실제 기능으로 구현한다. |
| **P2** | 후원 처리 신뢰성 | 중복 실행, 유실, 폭주, 잘못된 대상 지정을 방지한다. |
| **P3** | 코드 품질·테스트 | 타입 안정성, 모듈 경계, 재현 가능한 검증을 유지한다. |
| **P4** | 편의 기능·UI | 상위 규칙을 훼손하지 않는 범위에서만 개선한다. |

충돌 예시:

- 빠른 구현과 저장 안정성이 충돌하면 저장 안정성을 선택한다.
- 사용자 요청과 공식 API 명세가 충돌하면 실제 API를 조작해 맞추지 말고 제한 사항을 보고한다.
- 후원 이벤트 재시도와 중복 실행 위험이 충돌하면 자동 재시도를 중단하고 수동 확인 상태로 전환한다.

---

## 2. 프로젝트 목표

처리 흐름은 다음으로 고정한다.

```text
CHZZK OAuth
  → CHZZK Session 연결
  → DONATION 구독
  → 후원 페이로드 검증·정규화
  → 중복 판정
  → 금액별 규칙 결정
  → 영속 대기열 등록
  → 대상 플레이어 확인
  → OfficialPalworldRestClient 또는 GameBridge 실행
  → 실행 결과 저장·공지·관측
```

후원 수신 콜백에서 게임 명령을 직접 실행하지 않는다. 수신, 규칙 결정, 작업 등록, 실제 실행을 분리한다.

---

## 3. 공식 기능 경계

### 3.1 Palworld 공식 REST API에서 사용 가능한 기능

아래 기능만 `OfficialPalworldRestClient`에 구현한다.

| 분류 | 메서드와 경로 | 용도 |
|---|---|---|
| 조회 | `GET /v1/api/info` | 서버 정보 조회 |
| 조회 | `GET /v1/api/players` | 접속 플레이어 조회 |
| 조회 | `GET /v1/api/settings` | 현재 서버 설정 조회 |
| 조회 | `GET /v1/api/metrics` | 서버 지표 조회 |
| 관리 | `POST /v1/api/announce` | 공지 전송 |
| 관리 | `POST /v1/api/kick` | 플레이어 강퇴 |
| 관리 | `POST /v1/api/ban` | 플레이어 밴 |
| 관리 | `POST /v1/api/unban` | 밴 해제 |
| 저장 | `POST /v1/api/save` | 월드 저장 요청 |
| 종료 | `POST /v1/api/shutdown` | 대기 후 정상 종료 |
| 종료 | `POST /v1/api/stop` | 강제 종료 |

### 3.2 공식 REST API로 구현하지 않는 기능

다음 기능은 공식 REST API 기능으로 구현하거나 설명하지 않는다.

- 아이템 지급 또는 회수
- 팰 지급, 포획 처리 또는 소환
- 적·보스·레이드 생성
- 체력, 스태미나, 레벨, 경험치 직접 변경
- 버프·디버프 적용
- 인벤토리 조작
- 순간이동
- 서버 설정을 실행 중에 변경
- 현재 공식 개별 명세에서 확인되지 않은 후보 경로 사용

`GET /settings`는 조회 기능이다. 설정 변경이 필요하면 `PalWorldSettings.ini` 변경과 통제된 서버 재시작 절차로 분리한다.

### 3.3 GameBridge 경계

인게임 조작은 별도 인터페이스 뒤에 둔다.

```ts
export interface GameBridge {
  healthCheck(): Promise<GameBridgeHealth>;
  giveItem(command: GiveItemCommand): Promise<GameCommandResult>;
  spawnPal(command: SpawnPalCommand): Promise<GameCommandResult>;
  applyEffect(command: ApplyEffectCommand): Promise<GameCommandResult>;
  executeConfiguredEvent(
    command: ConfiguredEventCommand,
  ): Promise<GameCommandResult>;
}
```

구현체:

- `MockGameBridge`: 단위·통합 테스트용
- `HttpModGameBridge`: 서버 모드가 제공하는 로컬 HTTP 인터페이스용
- `DisabledGameBridge`: 모드가 없거나 지원되지 않는 환경에서 명시적으로 실패

`OfficialPalworldRestClient`에 인게임 조작 메서드를 추가하지 않는다.

---

## 4. P0 — 저장 데이터 보호

### 4.1 절대 금지

- 실행 중인 서버의 저장 파일을 직접 편집하지 않는다.
- 저장 폴더를 대상으로 검증되지 않은 변환 스크립트를 실행하지 않는다.
- 백업 없이 모드를 설치·업데이트·제거하지 않는다.
- `/stop`을 정상 종료의 기본 수단으로 사용하지 않는다.
- 실제 서버에서 파괴적 테스트를 자동 실행하지 않는다.
- 저장 성공 응답을 받지 못한 상태에서 안전하게 저장됐다고 기록하지 않는다.

### 4.2 설정·모드 변경 절차

설정이나 모드를 변경하는 코드를 작성할 때 다음 순서를 강제한다.

1. REST 상태 확인: `/info`, `/metrics`
2. 신규 후원 작업 수신 중지
3. 대기열의 실행 중 작업 종료 또는 명시적 취소
4. `POST /save` 호출
5. HTTP `200` 확인
6. 저장 디렉터리의 별도 백업 생성
7. 백업의 생성 시각, 크기, 파일 목록 기록
8. `POST /shutdown`으로 정상 종료
9. 프로세스 종료 확인
10. 설정 또는 모드 변경
11. 서버 재시작
12. `/info`, `/settings`, `/metrics` 확인
13. GameBridge 상태 확인
14. 테스트 후원 1건 실행
15. 이상이 없을 때 신규 후원 수신 재개

어느 단계든 실패하면 다음 단계로 진행하지 않고 실패 상태와 복구 방법을 기록한다.

### 4.3 공식 백업 설정

`bIsUseBackupSaveData`의 현재 값을 `/settings`로 확인한다. 프로덕션에서 비활성화되어 있으면 경고를 출력하되, 사용자 승인 없이 설정 파일을 자동 변경하지 않는다.

백업 기능이 켜져 있다는 이유로 별도 배포 전 백업을 생략하지 않는다.

---

## 5. P0 — 네트워크와 인증

### 5.1 Palworld REST API

- `RESTAPIEnabled=True`가 전제다.
- HTTP Basic Auth를 사용한다.
- REST API 포트를 공개 인터넷에 직접 노출하지 않는다.
- 기본 배치는 `localhost`, LAN, 사설망 또는 VPN 내부로 제한한다.
- 외부 게이트웨이가 필요하면 역방향 프록시, TLS, 접근 제어, 속도 제한을 추가한다.
- Basic Auth 자격 증명을 URL, 로그, 오류 메시지, 저장소에 기록하지 않는다.
- `RCON` 신규 기능을 작성하지 않는다. 공식 문서에서 폐기 예정으로 안내되므로 REST API 또는 GameBridge를 사용한다.

### 5.2 치지직 OAuth

- `clientSecret`, access token, refresh token을 저장소에 커밋하지 않는다.
- access token 만료에 대비해 refresh token 갱신 경로를 구현한다.
- refresh token을 재사용 가능한 영구 키로 가정하지 않는다.
- OAuth `state`를 생성하고 콜백에서 동일성 검증을 수행한다.
- 토큰 저장 시 평문 로그 출력을 금지한다.
- `401 INVALID_TOKEN`은 무한 재시도하지 않고 한 번의 안전한 갱신 흐름으로 연결한다.

### 5.3 관리자 API

- 개발용 후원 시뮬레이터와 비상 정지 API에도 인증을 적용한다.
- 관리자 요청에는 감사 로그를 남긴다.
- 밴, 서버 종료, 규칙 변경은 일반 후원 처리 권한과 분리한다.
- 후원 금액만으로 영구 밴, 강제 종료, 저장 삭제를 실행하지 않는다.

---

## 6. P1 — 치지직 후원 수신 규칙

### 6.1 세션

- 후원 조회 권한이 포함된 사용자 Access Token으로 세션을 생성한다.
- Socket.IO 연결 URL은 API에서 받은 값을 사용하고 하드코딩하지 않는다.
- 공식 지원 범위를 벗어나는 Socket.IO 클라이언트 버전을 임의로 사용하지 않는다.
- 현재 문서 기준 지원 범위는 `1.0.0` 이상 `2.0.3` 이하이다.
- 연결 완료 `SYSTEM` 메시지의 `sessionKey`를 받은 뒤 후원 이벤트를 구독한다.
- `subscribed` 확인 전에는 수신 준비 완료 상태로 표시하지 않는다.
- `revoked` 수신 시 해당 구독을 즉시 중지하고 재인증 필요 상태로 전환한다.
- 재연결 시 세션 생성부터 구독 확인까지 다시 수행한다.

### 6.2 DONATION 페이로드

공식 필드만 원본 스키마에 둔다.

```ts
export interface ChzzkDonationPayload {
  donationType: 'CHAT' | 'VIDEO';
  channelId: string;
  donatorChannelId: string;
  donatorNickname: string;
  payAmount: string;
  donationText: string;
  emojis: Record<string, string>;
}
```

검증 규칙:

- `payAmount`는 문자열로 수신한다.
- `^[1-9][0-9]*$`에 맞지 않으면 거부한다.
- `Number.MAX_SAFE_INTEGER`를 넘으면 거부한다.
- 프로젝트의 `MAX_DONATION_AMOUNT_KRW`를 넘으면 자동 실행하지 않고 검토 대기 상태로 둔다.
- 닉네임은 표시용이며 고유 식별자로 사용하지 않는다.
- 후원자 식별에는 `donatorChannelId`를 사용한다.
- `donationText`와 닉네임은 신뢰할 수 없는 외부 입력이다.
- 후원 메시지를 셸 명령, SQL, 파일 경로, 클래스명, 함수명 또는 게임 콘솔 명령으로 직접 연결하지 않는다.

공식 후원 페이로드에 문서화된 고유 후원 ID가 없으므로 `donationId` 필드가 존재한다고 가정하지 않는다.

---

## 7. P2 — 중복 방지와 멱등성

### 7.1 후원 지문

공식 고유 ID가 없을 때는 다음 값으로 지문을 생성한다.

```text
channelId
+ donatorChannelId
+ payAmount
+ donationType
+ donationText의 정규화 해시
+ 수신 시간 버킷
+ 세션 식별 정보
```

지문은 휴리스틱이며 정확한 일회 실행 보장을 제공한다고 설명하지 않는다.

### 7.2 저장 규칙

- 원본 후원, 정규화 결과, 지문, 수신 시각을 게임 실행 전에 영속화한다.
- 프로세스 메모리의 `Set`만으로 중복 방지를 구현하지 않는다.
- 데이터베이스에 지문 고유 제약 또는 동등한 원자적 삽입을 사용한다.
- 모든 게임 명령에 별도 `commandId`를 부여한다.
- GameBridge는 동일 `commandId` 재수신 시 이전 결과를 반환하거나 중복 실행을 거부해야 한다.

### 7.3 상태 모델

```text
received
→ validated
→ queued
→ running
→ succeeded | failed | manual_review | cancelled
```

상태를 건너뛰거나 실패를 성공으로 변환하지 않는다.

---

## 8. P2 — 금액별 이벤트 규칙

규칙은 코드의 `if/else` 연쇄가 아니라 검증 가능한 설정으로 관리한다.

```json
{
  "id": "give-item-1000",
  "enabled": true,
  "priority": 100,
  "amount": {
    "type": "range",
    "min": 1000,
    "max": 1999
  },
  "target": {
    "type": "streamer"
  },
  "action": {
    "type": "give_item",
    "itemId": "VERIFIED_ITEM_ID",
    "quantity": 10
  },
  "cooldownSeconds": 0
}
```

필수 검증:

- 규칙 ID 중복 금지
- 활성 규칙의 금액 구간 중복 금지
- `min <= max`
- 음수·0원 구간 금지
- 액션별 최대 수량·레벨·반경·지속 시간 제한
- 알 수 없는 액션 타입 거부
- 검증되지 않은 아이템 ID와 팰 ID 거부
- 동일 규칙 재사용 대기시간 적용
- 보스·대량 소환 이벤트는 전역 재사용 대기시간 추가
- 랜덤 이벤트는 허용 목록에서만 선택
- 랜덤 선택 결과와 난수 시드를 실행 로그에 기록

공식 또는 프로젝트 내부에서 검증된 ID 목록이 없으면 실제 ID를 창작하지 않는다. 인터페이스와 예시 자리표시자만 제공한다.

---

## 9. P2 — 대상 플레이어 결정

대상은 다음 방식만 허용한다.

- 고정된 방송인 `userId`
- 관리자가 등록한 Palworld `userId`
- 사전 등록된 `donatorChannelId ↔ Palworld userId` 매핑
- 현재 `/players` 응답에서 확인한 사용자

규칙:

- 이름이나 닉네임만으로 자동 매칭하지 않는다.
- Palworld 관리 동작에는 가능한 경우 `userId`를 사용한다.
- 후원자의 치지직 채널 ID와 Palworld 계정이 같다고 가정하지 않는다.
- 대상 미접속 시 정책은 `fail`, `hold_until`, `fallback_to_streamer` 중 하나로 설정에 명시한다.
- `hold_until`에는 최대 보류 시간을 둔다.
- 대상 변경은 실행 직전에 다시 검증한다.

---

## 10. P2 — 이벤트 대기열과 제한

필수 기능:

- 제한된 동시 실행 수
- 작업별 타임아웃
- 재시도 횟수 상한
- 지수 백오프와 지터
- 재시도 가능 오류와 불가능 오류 구분
- 전역 비상 정지
- 이벤트 종류별 재사용 대기시간
- 분당 최대 명령 수
- 분당 최대 소환 개체 수
- 서버 FPS 또는 상태가 임계값 아래일 때 자동 일시정지

다음 작업은 기본적으로 자동 재시도하지 않는다.

- 아이템 지급 결과가 불명확한 요청
- 팰·보스 소환 결과가 불명확한 요청
- 플레이어 대상이 실행 도중 변경된 요청
- 서버 종료·밴·데이터 변경 성격의 요청

---

## 11. P3 — 권장 코드 구조

기존 저장소 구조가 있으면 유지한다. 신규 TypeScript 프로젝트라면 다음 경계를 사용한다.

```text
src/
├─ chzzk/             # OAuth, Session, DONATION 어댑터
├─ donations/         # 원본 이벤트, 정규화, 영속화, 중복 판정
├─ rules/             # 규칙 스키마, 로더, 매처
├─ queue/             # 작업 상태와 워커
├─ palworld-rest/     # 공식 REST API 전용
├─ game-bridge/       # 인게임 모드 인터페이스와 구현체
├─ players/           # 치지직-Palworld 계정 매핑
├─ admin/             # 비상 정지, 시뮬레이터, 운영 API
├─ observability/     # 구조화 로그, 상태, 지표
└─ test-support/      # 모의 치지직·모의 Palworld 서버
```

의존 방향:

```text
도메인 ← 애플리케이션 서비스 ← 외부 어댑터
```

도메인 로직이 Socket.IO, HTTP 프레임워크, 파일 시스템 또는 특정 데이터베이스 SDK를 직접 호출하지 않게 한다.

---

## 12. P3 — TypeScript 규칙

신규 프로젝트 기본값:

- Node.js LTS
- TypeScript `strict: true`
- ESM
- `pnpm`
- 런타임 스키마 검증
- 구조화 JSON 로그
- 단위 테스트와 통합 테스트 분리

코드 규칙:

- 외부 입력은 `unknown`으로 받고 검증 후 사용한다.
- `any`, 무분별한 타입 단언, `@ts-ignore`로 오류를 숨기지 않는다.
- 오류를 문자열 비교만으로 분류하지 않는다.
- 네트워크 오류, 인증 오류, 검증 오류, 대상 없음, 타임아웃, 중복 명령을 구분한다.
- 빈 `catch` 블록을 사용하지 않는다.
- 비밀값 전체를 오류 객체에 첨부하지 않는다.
- 날짜와 시간은 저장 시 UTC를 사용하고 표시 시 명시적으로 변환한다.
- 금액 단위는 원 정수로 처리하며 부동소수점 연산을 사용하지 않는다.

새 의존성을 추가하기 전에 공식 유지보수 상태, TypeScript 지원, 라이선스, 보안, 기존 의존성으로 대체 가능한지를 확인한다.

---

## 13. P3 — 테스트 기준

### 13.1 단위 테스트

최소 사례:

- 정상 `payAmount` 변환
- 0, 음수, 소수, 공백, 지수 표기, 비숫자 거부
- 안전 정수 초과 거부
- 금액 경계값
- 규칙 구간 중복 감지
- 알 수 없는 액션 거부
- 허용 목록 밖 ID 거부
- 후원 지문 동일·상이 사례
- 재사용 대기시간
- 비상 정지
- 대상 미접속 정책
- `revoked` 상태 전환

### 13.2 통합 테스트

- 모의 CHZZK 세션 연결 → `SYSTEM connected` → 구독 → `DONATION` 수신
- 후원 수신 → 영속화 → 규칙 결정 → 대기열 → `MockGameBridge`
- Palworld REST Basic Auth 성공·실패
- `/players`, `/settings`, `/save`, `/shutdown` 계약 테스트
- GameBridge 타임아웃과 중복 `commandId`
- 프로세스 재시작 후 중복 방지

### 13.3 실제 서버 테스트

실제 Palworld 서버 테스트는 명시적 환경 변수와 수동 승인 없이는 실행하지 않는다.

테스트 순서:

1. 조회 전용 API
2. 공지
3. 저장
4. 모의 GameBridge
5. 안전한 단일 아이템 이벤트
6. 안전한 단일 소환 이벤트

밴, 강제 종료, 대량 소환은 자동 테스트 대상에서 제외한다.

---

## 14. 개발용 후원 시뮬레이터

- 프로덕션 기본 비활성화
- 관리자 인증 필수
- 실제 후원과 동일한 검증·정규화·규칙 엔진을 사용
- 시뮬레이션 여부를 별도 필드로 기록
- 실제 결제 이벤트 저장소와 구분 가능해야 함
- 임의 액션 직접 호출보다 `DONATION` 페이로드 입력 방식을 우선

시뮬레이터가 GameBridge 제한, 재사용 대기시간, 비상 정지를 우회하지 못하게 한다.

---

## 15. 관측성과 운영 상태

상태 API 또는 대시보드에서 다음을 확인할 수 있어야 한다.

- CHZZK OAuth 상태
- access token 만료 예정 시각
- Session 연결 상태
- DONATION 구독 확인 상태
- 마지막 후원 수신 시각
- 마지막 성공·실패 작업
- 대기열 크기
- Palworld REST 연결 상태
- GameBridge 연결 상태
- 서버 FPS와 접속자 수
- 비상 정지 상태와 사유

모든 로그에 가능한 범위에서 다음 상관관계 값을 포함한다.

- `donationFingerprint`
- `jobId`
- `commandId`
- `ruleId`

후원 메시지 원문과 개인정보는 운영에 필요한 기간보다 오래 보존하지 않는다.

---

## 16. 작업 절차

Codex는 작업마다 다음 순서를 따른다.

1. 이 파일과 하위 `AGENTS.md` 확인
2. 저장소 구조와 기존 스크립트 확인
3. 공식 문서와 코드의 차이 확인
4. 변경 범위와 위험 등급 결정
5. P0 작업이면 백업·롤백 계획 작성
6. 최소 범위로 구현
7. 타입 검사, 린트, 관련 테스트 실행
8. 변경 diff 검토
9. 문서와 `.env.example` 갱신
10. 실제 실행 여부와 미검증 부분을 분리해 보고

사용자가 계획만 요청하지 않았다면, 치명적 정보가 부족하지 않은 범위에서 계획 이후 구현과 검증까지 진행한다.

---

## 17. P1 — 참조 자료, 데이터 출처, 최신성

Codex는 외부 사실을 구현에 반영하기 전에 `docs/REFERENCE_SOURCES.md`와 `docs/VERIFIED_BASELINE.md`를 읽는다.

### 17.1 출처 우선순위

1. Pocketpair·Palworld·CHZZK의 공식 문서와 공식 공지
2. 대상 라이브러리·모드 프레임워크 유지관리자의 공식 저장소, 릴리스, 문서
3. 저장소에 커밋된 버전 고정 스냅샷과 실제 테스트 서버의 관측 결과
4. 신뢰 가능한 커뮤니티 위키·모딩 문서
5. 검색 결과, 게시글, 영상, 댓글

하위 출처는 상위 출처를 덮어쓰지 않는다. 상위 출처끼리 충돌하면 다음을 모두 기록한다.

- 확인한 정확한 URL
- 페이지에 표시된 버전
- 확인 날짜
- 실제 서버 또는 코드에서 관측한 결과
- 채택한 해석과 보류한 해석

### 17.2 최신성 규칙

- Palworld 패치, 서버 API, 모드 배포 방식, CHZZK 인증·세션 스키마는 관련 코드를 변경하는 작업마다 공식 문서를 다시 확인한다.
- 문서의 사이트 전체 버전 표기와 개별 페이지 버전 표기가 다르면 개별 페이지와 실제 서버 응답을 우선한다.
- 공식 개별 명세 페이지에서 확인되지 않은 경로는 과거 자료나 검색 결과에 등장하더라도 `unverified`로 둔다.
- 라이브러리 버전은 `package.json`만 보지 않고 잠금 파일, 공식 릴리스 노트, 지원 범위를 함께 확인한다.
- 마지막 검증 날짜가 없는 외부 사실은 검증되지 않은 것으로 취급한다.
- 확인할 수 없는 변경 가능 정보는 추측하지 않고 `unverified` 상태로 남긴다.

### 17.3 Palworld 내부 ID 데이터

공식 공개 문서에는 모든 아이템·팰의 내부 식별자를 보장하는 완전한 카탈로그가 없다고 전제한다.

프로덕션 허용 목록에 사용할 내부 ID는 다음 조건을 모두 충족해야 한다.

1. 대상 `Palworld v1.0.1` 빌드의 에셋, 데이터 테이블 또는 검증된 런타임 열거 결과에서 얻음
2. `data/palworld/v1.0.1/` 아래 버전 고정 파일로 저장됨
3. 출처, 추출 시각, 추출 도구 버전, 원본 해시가 기록됨
4. 복사한 테스트 월드에서 실제 지급 또는 소환을 검증함
5. 검증 결과와 실패 조건이 기록됨

위키의 표시 이름, 번역명, 검색 결과만으로 내부 ID를 생성하지 않는다. 커뮤니티 데이터는 후보 탐색에만 사용하고 실제 에셋 또는 테스트 서버로 재검증한다.

### 17.4 문서화와 인용

외부 명세를 근거로 코드를 변경하면 PR 또는 최종 보고에 다음을 남긴다.

- 사용한 출처
- 확인 날짜
- 대상 게임·서버·라이브러리 버전
- 명세에서 직접 확인한 필드 또는 동작
- 추론한 부분과 실제 검증한 부분의 구분

`docs/REFERENCE_SOURCES.md`에 없는 새 출처를 사용했다면 신뢰 등급과 사용 목적을 검토한 후 해당 문서를 함께 갱신한다.
